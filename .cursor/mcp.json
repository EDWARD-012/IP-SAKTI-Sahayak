{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp@latest"],
      "description": "Browser automation — test Django chat UI, fill forms, take screenshots"
    },
    "sqlite": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sqlite", "db.sqlite3"],
      "description": "Inspect AnswerAudit, CorpusVersion, WizardSession tables while the app is running"
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"],
      "description": "Live up-to-date docs for Django 5, LangChain, bge-m3, Chroma, Ollama — eliminates hallucinated APIs"
    },
    "firecrawl": {
      "command": "npx",
      "args": ["-y", "firecrawl-mcp"],
      "env": {
        "FIRECRAWL_API_URL": "https://mcp.firecrawl.dev/v2/mcp"
      },
      "description": "Keyless web scraping — fetch India Code, TKDL, WIPO, FSSAI pages as clean markdown for corpus building (1000 free credits/month)"
    },
    "india-law": {
      "command": "npx",
      "args": ["-y", "@ansvar/india-law-mcp"],
      "description": "846 Indian Acts searchable locally — validate citations, check if a section is in force, cross-reference Acts. DIRECTLY USEFUL for IP-SAKTI citation validator."
    }
  }
}
