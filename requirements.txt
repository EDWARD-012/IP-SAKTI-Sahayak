# IP-SAKTI Sahayak — Python dependencies
# Python 3.11+ required
# Install: pip install -r requirements.txt

# ── Django core ──────────────────────────────────────────────
Django>=5.1,<6
django-htmx>=1.19
django-csp>=3.8

# ── Settings / config ─────────────────────────────────────────
pydantic-settings>=2.4
python-dotenv>=1.0

# ── Static files in production ────────────────────────────────
whitenoise[brotli]>=6.7

# ── Production WSGI servers ───────────────────────────────────
gunicorn>=22.0      # Linux / Docker / Render
waitress>=3.0       # Windows demo

# ── Database (optional Postgres on Railway) ───────────────────
psycopg[binary]>=3.1

# ── AI / RAG stack ────────────────────────────────────────────
langchain-chroma>=0.1
langchain-ollama>=0.2
langchain-core>=0.3
sentence-transformers>=3.2    # BAAI/bge-m3 embeddings
chromadb>=0.5
ollama>=0.3                   # Python client for Ollama

# ── Corpus ingestion ──────────────────────────────────────────
pypdf>=4.3                    # Legal PDF parsing
pdfminer.six>=20221105        # Fallback PDF text extraction
PyYAML>=6.0                   # Source manifest files (data/manifests/*.yaml)

# ── Optional: translation (IndicTrans2, all 22 languages) ─────
# Heavy (torch + transformers, ~5 GB models on first run). Uncomment to enable:
# torch>=2.2
# transformers>=4.44
# IndicTransToolkit @ git+https://github.com/VarunGumma/IndicTransToolkit

# ── Utilities ─────────────────────────────────────────────────
requests>=2.32
tqdm>=4.66
